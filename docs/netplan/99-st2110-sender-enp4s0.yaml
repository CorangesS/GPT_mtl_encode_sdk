# 发送端（本机）固定 IP：enp4s0 = 192.168.10.1
# 用法：sudo cp 此文件到 /etc/netplan/ 后执行 sudo netplan apply
# 例如：sudo cp docs/netplan/99-st2110-sender-enp4s0.yaml /etc/netplan/
network:
  version: 2
  ethernets:
    enp4s0:
      addresses: [192.168.10.1/24]
